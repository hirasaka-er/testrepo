# .github/workflows/test.yml
name: Test Workflow

on:
  workflow_dispatch:  # 手動実行可能

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - id: test1
        run: |
          ls .
      - id: test2
        run: |
          ls ${{ github.workspace }}
