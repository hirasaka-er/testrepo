# .github/workflows/test-deploy.yml
name: Test Deploy Workflow

on:
  workflow_dispatch:  # 手動実行可能

jobs:
  test_deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      env: "staging"  # ここを自由に変更可能
    secrets:
      TOKEN: ${{ secrets.DEPLOY_TOKEN_TEST }}
      ENV_JSON: ${{ secrets.ENV_JSON }}
